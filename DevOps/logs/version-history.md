# Version History & Policy

This log records formal releases, deployments, and reviewed main-branch pushes for
RD-API-Server.

## 📌 Semantic Versioning

Public releases use `vMAJOR.MINOR.PATCH` Git tags and standard Semantic Versioning:

1. **Major** (`v1.0.0` → `v2.0.0`): incompatible application, deployment, database, or client-contract boundaries.
2. **Minor** (`v1.0.0` → `v1.1.0`): backward-compatible features and substantial new workflows.
3. **Patch** (`v1.0.0` → `v1.0.1`): backward-compatible fixes, hardening, and maintenance.

Main-branch pushes do not automatically create releases or increment the application version.
A release requires an explicitly reviewed version change, a verified release commit, an annotated
tag, published versioned container images, and a GitHub Release.

---

## Main Branch Delivery Log

| Date | Version Impact | Actor | Delivery |
|---|---|---|---|
| 2026-07-18 | v1.1.0 | GigaionLLC / OpenAI Codex | Published verified release commit `3755425` as annotated tag `v1.1.0` and the [GitHub Release](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.1.0). [Main CI run 29671761606](https://github.com/GigaionLLC/RD-API-Server/actions/runs/29671761606) and [tag CI run 29671977593](https://github.com/GigaionLLC/RD-API-Server/actions/runs/29671977593) passed the full PHP, JavaScript, browser, production-runtime, native AMD64/ARM64, live-digest, and final-manifest gates. Stable tags `1.1.0`, `1.1`, `1`, `latest`, and the full-SHA tag resolve to manifest digest `sha256:7ed0b357f2ab3d27a793f851cfc5458f2a16b0f1c00693b5eb6da373aafe90a0`. The release replaces Apache with Nginx/PHP-FPM and adds warned wildcard proxy trust without schema, storage, port, or RustDesk wire-contract changes; full fleet-capacity and public 1Panel qualification remain explicitly pending. |
| 2026-07-18 | Unchanged (`v1.1.0` candidate) | OpenAI Codex / GPT-5 | User-authorized main-branch delivery `be1bdf7`, `47d7820`, `156f740`, and `6da6fff` added warned `TRUSTED_PROXIES=*` support, made wildcard trust the unset bundled-Compose default, documented exact proxy IP/CIDR restrictions, and isolated the untrusted-header PHPUnit baseline from deployment defaults. The initial CI run identified that copied `.env.example` state leaked into two security tests; the focused test-isolation follow-up resolved it. [CI run 29670381387](https://github.com/GigaionLLC/RD-API-Server/actions/runs/29670381387) then passed PHP, JavaScript, browser, native AMD64/ARM64 build, live digest smoke, and manifest gates. Full-SHA candidate `sha-6da6fffc4879d67bd95e7704f41b063b0ef237e4` was published at multi-platform digest `sha256:de15e27cefe8cce44dcb6fdf79cee85519c35f3d4a0ceea4363b89291ee28136`; stable `latest` remains v1.0.1. No application version, schema, database, port, or RustDesk wire-contract change. |
| 2026-07-18 | Unchanged (`v1.1.0` candidate) | OpenAI Codex / GPT-5 | User-authorized reviewed main-branch delivery of release-channel guard `ac293f9`, Nginx/PHP-FPM candidate `2627aa6`, source-parity capacity harness `5eb304e`, native runtime CI `1850b9a`, and independently revertible runtime package cleanup `d51c0da`. [CI run 29659015951](https://github.com/GigaionLLC/RD-API-Server/actions/runs/29659015951) passed PHP, JavaScript, browser, native AMD64/ARM64 build, live digest smoke, and manifest gates. Commit `65dc8c9` is available only as full-SHA candidate tag `sha-65dc8c9b9fc3be47b7bafc910acabac100f6f5cf` at multi-platform digest `sha256:40d82e1ca22ae87b71d759b84b09e244dae14b61c1d86a361d3ee36aa3233ee4`; stable `latest` remains v1.0.1 at `sha256:65fdd380ab101ef8fcf40e8281aa303257559f3da4008dfb00782138e71268e2` pending the complete 1,800-RPS matrix and public 1Panel canary. No application version, schema, API, storage, port, or proxy-contract change. |
| 2026-07-17 | v1.0.1 | OpenAI Codex / GPT-5 | Published verified commit `8a3f708` as annotated tag `v1.0.1` and the [GitHub Release](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.0.1). Main CI run `29627965974` and tag CI run `29628204395` passed the complete quality matrix, native AMD64/ARM64 digest smoke tests, and final-manifest verification. GHCR SemVer tags resolve to manifest digest `sha256:65fdd380ab101ef8fcf40e8281aa303257559f3da4008dfb00782138e71268e2`. The new pipeline completed in 7 minutes 11 seconds on main and 5 minutes 49 seconds warm on the tag. No database schema or RustDesk wire-contract change. |
| 2026-07-17 | v1.0.0 | OpenAI Codex / GPT-5 | Published verified commit `026b841` as annotated tag `v1.0.0` and the first stable [GitHub Release](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.0.0). CI run `29626539704`, main-image run `29626539712`, and tag-image run `29626681147` passed. GHCR tags `1.0.0`, `1.0`, and `1` contain AMD64 and ARM64 at digest `sha256:512c1fb8b40ff72cb71fe1a66c872198741f1ac6d08a4c0c0f00ee5877949705`. Production also passes the public HTTPS proxy checker after trusting its exact application-observed 1Panel peer and enabling secure cookies. |
| 2026-07-17 | Unchanged | OpenAI Codex / GPT-5 | User-authorized source delivery `52410f9` and `4c28f08` restricted the trusted forwarded-header surface and added HTTPS proxy diagnostics, safe deployment guidance, secure-cookie configuration, and a fail-closed public smoke check. The full Docker matrix passed 538 PHPUnit tests / 3,051 assertions plus static and packaging gates; GitHub CI run `29623089296` and Docker Publish run `29623089305` also completed successfully. No formal release or production deployment was performed; the public origin still requires its application-observed proxy IP/CIDR before the mixed-content incident is resolved. |
| 2026-07-15 | Unchanged | OpenAI Codex / GPT-5 | CI-maintenance push `22dbe25` replaced deprecated Node 20 checkout/setup actions with immutable Node 24-native v7.0.0 pins. GitHub CI run `29470504915` and multi-architecture Docker Publish run `29470504901` passed without the prior deprecation annotation. No formal release or application deployment was performed. |
| 2026-07-15 | Unchanged | OpenAI Codex / GPT-5 | User-authorized completion push to `origin/main` for the independently revertible WebUI modernization, MariaDB-only boundary, review remediation, and security hardening series. Final Docker gates passed 532 PHPUnit tests / 3,018 assertions and 68 Playwright tests with 12 intentional skips; static, packaging, and dependency-audit gates were green. No deployment or formal release was performed. |

## 📈 Release Log

| Version | Date | Level | Deployer | Key Release Highlights / Milestones |
|---|---|---|---|---|
| **[v1.6.2](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.6.2)** | 2026-08-17 | Patch | GigaionLLC / Claude Opus 5 | Fixes clicks landing away from the drawn pointer, reported from a live deployment. Two code paths derived the same relationship independently — the cursor layer used the measured ratio between video size and the display's reported size, input divided by `DisplayInfo.scale` — and those are inverses only by coincidence, so the pointer could be drawn in one place while the click arrived in another. Input is now the exact inverse of the drawing, making disagreement structurally impossible and removing any assumption about what `scale` means per platform; it collapses to a plain offset when video and display report the same size, which is pinned separately so correct deployments are unaffected. Adds a remote-cursor toggle and a pointer readout in the statistics overlay so the mapping is visible rather than inferred. AMD64/ARM64 images at manifest digest `sha256:f3ee79d76afaacd31992b11a02a58b1418844c4cc7e30fb9336acbe410832fb3`. **Not verified against a live peer** — the available test machine is sending no video, so the mapping was checked by construction and in tests only. |
| **[v1.6.1](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.6.1)** | 2026-08-17 | Patch | GigaionLLC / Claude Opus 5 | Four browser remote-desktop fixes, three reported from live deployments. The viewer hid the operator's local pointer unconditionally while the peer suppresses cursor-position updates toward whoever sends input, so controlling meant aiming with nothing — the local pointer is now hidden only while watching and only once a remote one is drawn, and the peer's cursor is requested at login. A connection the far end has not accepted can now be cancelled (Disconnect lives in the toolbar, which only exists after a session is established), and a deliberate cancel is not retried as a dropped session. An upstream pasted into the browser-facing `RUSTDESK_WS_ID_URL` reached the browser while diagnostics called the deployment healthy; unusable endpoints are now named and withheld. A `peer_info` re-delivered on a monitor change no longer blanks the session header. Documents the UPSTREAM/URL distinction in the README, `.env.example` and the examples, and adds `examples/simple.docker-compose.yml` — the whole stack in one file with no `.env`. AMD64/ARM64 images at manifest digest `sha256:9dac880e4e441d480dcc13f23e9bd82b06467f43e14849d8d57cebe4af092ea7`. Verified against a live peer through the published image for cursor, cancel and identity; **video was not re-confirmed** — the test peer sent no frames, and the previous release behaved identically minutes apart, so it is that machine's state rather than a regression. |
| **[v1.6.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.6.0)** | 2026-08-17 | Minor | GigaionLLC / Claude Opus 5 | Shows the application version in the sidebar footer on every page — the first question on every support thread — and adds a **support report** at System -> Support report that assembles version, environment, configuration, browser remote-desktop diagnostics, database facts and the last 400 log lines into one paste for a GitHub issue. Everything passes through `LogRedactor` first: addresses, MACs, hostnames, whole URLs, emails, base64 key material, RustDesk peer ids and any value whose *name* implies a secret become consistent placeholders, so a reader can still follow one machine through a log while nothing identifying survives. Secret-by-name values are never read in order to be hidden; the report states whether a key is set and which half of the pair it is. Shown in full before it can be downloaded, and gated on `settings.view`. AMD64/ARM64 images at manifest digest `sha256:9f521a2fad39ed25176d0151751de8ec95f28a6595889313842a8ea65514d970`, with a report generated from the published image and checked for leaks. Two redaction defects were found by reading real output while the unit tests were green — a harmless `production.DEBUG:` pair swallowing a following `DB_PASSWORD=` assignment, and a kernel release being replaced because it parses as a valid IPv4 address — both now pinned. |
| **[v1.5.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.5.0)** | 2026-08-17 | Minor | GigaionLLC / Claude Opus 5 | Every console picker whose length is a function of deployment size now searches rather than rendering every row into the page: the remote peer picker, three user-group pickers, and three multi-value group pickers. The searchable combobox gained multi-value support (removable chips, Backspace to remove the last, duplicates refused), which allowed the last three multi-selects to convert; it emits whichever shape the form already posted — comma-joined field or one hidden input per value — so no controller, validation rule or write path changed. Fixed lists stay native selects, and CLAUDE.md now records the rule. Fixes the admin-role scope picker, which ran an unscoped query and named every group in the deployment to a delegate confined to one, and a dead query fetching every in-scope user on every device-index load. AMD64/ARM64 images at manifest digest `sha256:5b853a9e93e82a7e6ef24af656384319335b1c7434f234dedc92dab64eb53d99`, exercised in a browser after publication: search, select, deduplicate, remove, submit, and the saved row read back from the database. |
| **[v1.4.6](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.6)** | 2026-08-17 | Patch | GigaionLLC / Claude Opus 5 | Documentation and shipped examples only. The Compose files and `.env.example` were the last place naming `RUSTDESK_KEY` without saying which half of the pair it wants, and `.env.example` omitted the RustDesk server settings entirely — so an operator working from it had nothing to check a guess against, which is how the private key came to be configured in the first place. They now name `RUSTDESK_PUBLIC_KEY`, state that `id_ed25519.pub` is wanted and `id_ed25519` is not, and explain the consequence. Existing deployments need no edit: the Compose files read the new name and fall back to `RUSTDESK_KEY`, verified with `docker compose config` in all four combinations. The Compose files also now pass the four WebSocket settings through — the browser remote desktop previously could not be configured from them at all — and the full-stack example wires the container-carried transport and comments out the 21118/21119 publications that become unnecessary. No image behaviour change. |
| **[v1.4.5](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.5)** | 2026-08-16 | Patch | GigaionLLC / Claude Opus 5 | Security: this server can no longer distribute the RustDesk private key, however it is configured. It never signs anything — the key is held only to hand to clients and the viewer, which use it to verify — so the private half has no use here, yet could be configured and was then published to every client fetching a configuration, letting anyone holding it impersonate the ID server. Both distribution paths (client-config generator and browser viewer) now go through `ServerKeyService`, which yields only the public half and derives it from a supplied private key, so a misconfigured deployment works and is told rather than breaking with a puzzle; a value that is neither half is dropped rather than distributed. Adds `RUSTDESK_PUBLIC_KEY`/`RUSTDESK_PUBLIC_KEY_FILE`, with `RUSTDESK_KEY` retained indefinitely because breaking the ecosystem-conventional name would silently disable peer verification. Found on the operator's live deployment, where hbbs refused the connection. AMD64/ARM64 images at manifest digest `sha256:ef622af00af85e3f976973d0bd7ffcb95b8cfd649b9c544948c9d1b08616b62c`. Correcting the setting does not un-distribute an exposed key: affected deployments must rotate the pair. |
| **[v1.4.4](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.4)** | 2026-08-16 | Patch | GigaionLLC / Claude Opus 5 | The browser remote desktop works, and is verified end to end against a live peer for the first time: rendezvous, relay, encrypted handshake, login, H.265 at 1920x1080 with frames received equal to frames painted, four monitors enumerated, started by pressing Connect and run view-only. Fixes the last of three faults sharing one symptom: the viewer's HTML is served from an application route, so `src="./viewer.js"` resolved against that route rather than the assets directory and one 404 stopped the whole module graph; the document now carries a `<base>`, whose trailing slash `asset()` strips and without which the identical 404 recurs. Also adds a diagnostics check for a `RUSTDESK_KEY` holding the 64-byte private half of the Ed25519 pair — a real misconfiguration found on a live deployment, which publishes the key proving the server's identity to every client and is refused by hbbs; the check prints the correct public key and says to rotate. AMD64/ARM64 images are published at manifest digest `sha256:8b3561625c8d09bec29c2912fb2fd7cfb371a22ed43209322fdefa7f3e2d6254`, and the published image itself was used for the live-peer verification. |
| **[v1.4.3](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.3)** | 2026-08-16 | Patch | GigaionLLC / Claude Opus 5 | The browser remote desktop shipped in 1.4.0 had never run. The publisher flattened `src/*` to the document root while leaving `vendor/` beside it, so every vendored import (`../vendor/...` from `src/`, `../../vendor/...` from `src/crypto/` and `src/render/`) resolved a directory too high into `/assets/vendor/`; the browser 404ed, a failed import took the entire module graph with it, and the viewer rendered as bare HTML offering its manual connection form on a page whose configuration had been injected correctly. There is no build step, so the published layout has to equal the source; it now does in both the publisher and the image build, and the publisher clears its whole target so a previous layout cannot strand files at paths nothing serves. The seam went untested because every test loaded the viewer from `web-client/`, where the layout is correct by construction — two tests now resolve every import in the published tree the way a browser does and assert it is byte-identical to the source. Also fixed the feature suite deleting the published viewer on every run. AMD64/ARM64 images are published at manifest digest `sha256:57e2bfce387c3b64212ded67b6937751e8776c2188ff899bac4e934a3720ba99`. A full session against a live peer through the published copy is still unverified. |
| **[v1.4.2](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.2)** | 2026-08-16 | Patch | GigaionLLC / Claude Opus 5 | Adds a Remote control screen that takes a RustDesk ID directly with server details filled in from this deployment, and establishes that nothing connects on its own: Connect on a device opens that screen with the ID pre-filled and waits, and the viewer's auto-connect handling was removed outright rather than defaulted off so no configuration value or URL can restore it. An ID outside the device list is accepted only from an operator whose device permission is unrestricted, keeping the scope a boundary. Fixes three defects found by the operator's first real use: the viewer rendered as a 300x150 thumbnail because its stylesheet was pushed to a Blade stack the layout does not render; configuration injection into the viewer document failed silently and left it asking for an ID server and key the deployment already knew; and compiled Blade views in the persistent storage volume could survive an image upgrade. AMD64/ARM64 images are published at manifest digest `sha256:84d08d4c0de2724dc935dd51f56e7f512064c7f5d2bc04f1d65169c21683af61`. The proxied WebSocket transport from 1.4.1 remains unverified against a live hbbs behind a second reverse proxy. |
| **[v1.4.1](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.1)** | 2026-08-16 | Patch | GigaionLLC / Claude Opus 5 | Made the browser remote desktop deployable without a reverse-proxy change: `RUSTDESK_WS_ID_UPSTREAM` and `RUSTDESK_WS_RELAY_UPSTREAM` make the runtime serve `/ws/id` and `/ws/relay` on the console's own hostname and certificate and forward them to hbbs and hbbr, replacing a second certificate, two public ports and a hand-written WebSocket vhost with two environment values. The container thereby joins the media path, which is stated wherever the option is offered; explicit `RUSTDESK_WS_*_URL` values still win. Blanking `X-Real-IP`/`X-Forwarded-For` toward hbbs in the container removes the misconfiguration that makes concurrent operators behind one address take each other's connections. Upstreams resolve through an Nginx variable rather than a literal, so the container still boots when hbbs is slower to start and does not pin a stale address for the life of the process. Adds a System -> Remote desktop diagnostics page naming every condition that otherwise presents as 'cannot connect' against a healthy server, including a WebSocket probe from the operator's own browser, and the PHP route tests 1.4.0 shipped without. AMD64/ARM64 images are published at manifest digest `sha256:99658c5b876646fcdc8b40a7d3c7fc26b4aff391065b6cca86e590aac2429b82`. The v1.4.0 GitHub Release page was deleted at the owner's request so its superseded two-URL setup would not be followed; the tag and image remain, and its notes stay in `docs/releases/v1.4.0.md`. The proxied transport is verified through its rendered configuration and validation, not against a live hbbs behind a second reverse proxy. |
| **[v1.4.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.4.0)** | 2026-08-16 | Minor | GigaionLLC / Claude Opus 5 | A remote desktop that runs in the browser: **Connect** on any device opens a session against that peer — screen, sound, mouse, keyboard, clipboard and chat — with no plugin, download or client install. The viewer is a clean-room implementation under `web-client/` that speaks the RustDesk protocol directly to `hbbs` and `hbbr` over WebSocket, so this server is never in the media path and the feature adds no long-running process; it has no build step and runs the whole session in a Worker. Also adds automatic reconnection with a taxonomy that refuses to retry a wrong password or a refused encryption downgrade, surfaces the peer's message boxes, permission denials and Windows elevation state, and ships an encrypted notes vault plus a draft contributor assignment agreement. A release-blocking defect was caught during preparation: the published viewer lives at a gitignored path that nothing in the image build regenerated, so the image would have shipped a Connect button leading to a 404 — the build now publishes it and the runtime smoke fetches it back out of the image. AMD64/ARM64 images are published at manifest digest `sha256:2faa5e4cfbcd9746221e9366d2a3a56aa748280b605c1c21a3187b6e9f863cfa`. File transfer and terminal are not implemented; direct peer-to-peer is impossible from a browser, so every session is relayed. Verified against a single live Windows peer only — not exercised against macOS or Linux peers, multiple concurrent operators, or a reverse-proxied deployment. |
| **[v1.3.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.3.0)** | 2026-07-25 | Minor | GigaionLLC / Claude Opus 5 | Identity-provider groups can grant console roles, reconciled at sign-in with grant provenance so federated grants revoke on departure while hand-assigned roles survive; a designated break-glass administrator that no directory or SSO provider can demote, disable or lock out, closing a defect where `LDAP_SYNC=true` silently demoted a linked administrator on every sign-in whenever `LDAP_ADMIN_GROUP` was unset; `ADMIN_PASS` optional with a generated first-boot credential; and `rustdesk:admin:reset`, the only recovery path from a lost second factor. Published first as `v1.3.0-rc.1` through the new candidate channel, then promoted from the same commit `83308e4`. AMD64/ARM64 images are published at manifest digest `sha256:e7a117d04df70d06effacd7a8160f967b225ab015c1c9ea58ac3b3e30efea8cf`. Not exercised against a live FreeIPA or Authentik directory before release; the group-mapping feature is inert until an operator creates a mapping. |
| **[v1.2.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.2.0)** | 2026-07-24 | Minor | GigaionLLC / Claude Opus 5 | Made a self-hosted OIDC identity provider on a private network reachable through the deny-by-default `RUSTDESK_OIDC_ALLOWED_NETWORKS` / `RUSTDESK_OIDC_ALLOW_PRIVATE_NETWORKS` opt-ins, narrowed by a non-overridable block set and by only trusting a private address for the issuer's own host; replaced silent OIDC discovery failures with a reported reason; and fixed two host-resolution defects shared by the OIDC and webhook guards. Resolves [#1](https://github.com/GigaionLLC/RD-API-Server/issues/1). AMD64/ARM64 images are published at manifest digest `sha256:75d3b0e07af71c2c5e68be0b1bb86a5341cff298a50e297ed9e23995d25bd823`. End-to-end confirmation against a live private-network Authentik deployment is requested from the reporter and remains outstanding. |
| **[v1.1.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.1.0)** | 2026-07-18 | Minor | GigaionLLC / OpenAI Codex | Replaced Apache/mod_php with a supervised Nginx/PHP-FPM runtime while preserving the image contract, and added warned `TRUSTED_PROXIES=*` convenience with exact IP/CIDR restriction guidance. AMD64/ARM64 images are published at manifest digest `sha256:7ed0b357f2ab3d27a793f851cfc5458f2a16b0f1c00693b5eb6da373aafe90a0`. Fleet-scale capacity and the public 1Panel canary remain disclosed follow-up qualification rather than certified release evidence. |
| **[v1.0.1](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.0.1)** | 2026-07-17 | Patch | GigaionLLC / OpenAI Codex | Dark mode defaults on first visit while saved light mode persists; runtime images reuse one PHP extension layer and publish from native AMD64/ARM64 runners only after exact-commit quality and digest smoke gates pass. AMD64/ARM64 images are published at manifest digest `sha256:65fdd380ab101ef8fcf40e8281aa303257559f3da4008dfb00782138e71268e2`. |
| **[v1.0.0](https://github.com/GigaionLLC/RD-API-Server/releases/tag/v1.0.0)** | 2026-07-17 | Major | GigaionLLC / OpenAI Codex | First stable PHP 8.5/Laravel release with the modern WebUI, RustDesk-compatible client and administration APIs, MariaDB/InnoDB-only runtime, explicit reverse-proxy trust, and AMD64/ARM64 Docker images at manifest digest `sha256:512c1fb8b40ff72cb71fe1a66c872198741f1ac6d08a4c0c0f00ee5877949705`. |
